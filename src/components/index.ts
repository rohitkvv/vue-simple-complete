import Vue from "vue";
import SimpleComplete from "./SimpleComplete.vue";

const SimpleCompleteComponent = {
  SimpleComplete
};

Vue.component("vue-simple-complete", SimpleComplete);

export default SimpleCompleteComponent;
